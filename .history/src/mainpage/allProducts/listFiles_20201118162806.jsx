import React from 'react'

export default function listFiles({ dataValues }) {
    return (
        <div>
            {dataValue.map((link)=>(
                   <div>{link}</div>)
                )}
        </div>
    )
}
