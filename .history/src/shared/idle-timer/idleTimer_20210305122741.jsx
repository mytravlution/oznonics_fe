import React from 'react'
import IdleTimer from 'react-idle-timer'

function idleTimer() {
    cont IdleTimerRef = const ref = useRef(initialValue)
    return (
        <div>
            
        </div>
    )
}

export default idleTimer
