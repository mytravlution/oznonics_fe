import React from 'react'
import IdleTimer from 'react-idle-timer'

function idleTimer() {
    cont IdleTimerRef = useRef(null);
    
    return (
        <div>
            
        </div>
    )
}

export default idleTimer
